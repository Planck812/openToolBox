# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.2.0] - 2026-09-07

**首个可用的 macOS 版本**（Apple Silicon）。此前 macOS 用户只能自行源码构建，
而源码在 macOS 上实际编译不过 —— 本版把这条路彻底打通。

> v1.1.0 曾短暂发布但产物不可用：`.app` bundle 缺少签名，下载后被 macOS 判定为
> 「已损坏」且无法通过右键放行，该 tag 已删除。修复见下方「修复」一节末条。

### 新增

- **macOS 预编译包**：CI 增加 macOS 构建矩阵，发版时同时产出 Windows 安装包与 macOS DMG。
- **久坐提醒支持 macOS**：用 `CGEventSourceSecondsSinceLastEventType` 实现空闲检测，
  无需额外系统授权。此前该工具在 macOS 上是个能看到、点不动的空壳。
- **快捷唤起面板与久坐提醒弹窗可浮于全屏应用之上**：改用 NSPanel 并设置非激活样式，
  在全屏应用中也能就地弹出，且不会把用户从全屏里拽走。

### 修复

- **macOS 上无法编译**：`lib.rs` 无条件调用了 `#[cfg(windows)]` 门控的滚动截图模块，
  导致非 Windows 平台编译失败。
- **macOS 上主线程被打满、窗口完全失去响应**：无边框窗口下查询最大化状态会触发
  `setStyleMask`，进而再次派发 resize 事件，与 resize 回调中的状态查询形成自激循环。
  表现为窗口拖不动、标题栏按钮与快捷面板全部无响应。
- **macOS 上点击 Dock 图标无法唤起窗口**：主窗口关闭时隐藏到托盘，而此前未处理
  `RunEvent::Reopen`，导致应用已激活但窗口始终不出现。
- **小屏上窗口超出屏幕**：窗口尺寸固定 1600x800，在 13" MacBook 上比屏幕还宽，
  左右被裁掉。现按显示器工作区自适应并居中。
- **macOS 上密码夹「查看密码需验证身份」完全失效**：该校验在非 Windows 平台
  无条件放行 —— 落盘加密正常，但任何拿到已解锁设备的人都能直接读出全部密码。
  现接入 LocalAuthentication（Touch ID / Apple Watch / 登录密码），验证失败一律拒绝。
- **macOS 包下载后提示「已损坏」且无法打开**：缺少 `bundle.macOS` 配置导致 Tauri
  跳过签名，`.app` 仅有链接器临时签名而无资源封印。现配置 ad-hoc 签名，
  并在 CI 增加签名校验，不合格直接构建失败。

### 变更

- **密码夹存储格式升级至 v2**：文档明文落盘、仅密码字段单独加密。此前整个文档加密，
  导致列出条目也需读取主密钥，在 macOS 上每次打开都被钥匙串授权框打断。
  旧数据首次读取时自动迁移。
- **macOS 暂不提供以下工具**：全平台截图、便利贴（实现存在已知缺陷，
  与其带 bug 发布不如先停用）、端口占用杀手（依赖 Windows 专有命令）。
  对应的全局快捷键、托盘项与设置项一并隐藏。
- 首页分类导航补全：图片查看、Curl 调试台、OCR 文字识别、便利贴、久坐提醒、计时中心、AES 加解密 7 个工具现已归属对应分类。
- 修正 README 工具数量（27 → 30）并补全工具清单。
- README 补充 macOS 下载与安装指引，含 Gatekeeper 放行步骤与平台差异说明。

### 已知限制

- macOS 包为 **adhoc 签名**，首次打开需**右键 →「打开」**放行（双击不会出现放行选项）。
- macOS 包**仅支持 Apple Silicon**，Intel 机型需自行源码构建。
- 密码夹首次读取主密钥会请求钥匙串授权；选「始终允许」后不再打扰，
  但授权绑定应用二进制，升级新版本后需重新授权一次。
