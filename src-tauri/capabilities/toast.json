{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "toast",
  "description": "Capability for the toast notification window",
  "windows": ["toast"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
